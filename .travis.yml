language: python
python:
  - "3.6"
# command to run tests
services:
  - docker

install:
  - pip install codepod

before_install:
  - docker pull magland/codepod_spikeforest2
#- docker run -d -p 127.0.0.1:80:4567 carlad/sinatra /bin/sh -c "cd /root/sinatra; bundle exec foreman start;"
#- docker ps -a

# command to install dependencies
script:
  - ./codepod_run.sh