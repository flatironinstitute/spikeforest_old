{
  "name": "mtbrowser",
  "version": "0.1.0",
  "description": "MountainTools browser",
  "main": "server/index.js",
  "engines": {
    "yarn": "1.15.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && yarn build",
    "heroku-postbuild": "cd client && yarn install --dev && npm run build",
    "start": "node server/index.js"
  },
  "author": "Jeremy Magland",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^0.19.0",
    "express": "^4.17.1",
    "json-stable-stringify": "^1.0.1",
    "text-encoding": "^0.7.0"
  },
  "devDependencies": {
  }
}
